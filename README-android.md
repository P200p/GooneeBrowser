# 🦈 Gooser Browser (Goonee Browser)

**Gooser** คือเว็บเบราเซอร์ขนาดเล็กที่เน้นการควบคุมโดยผู้ใช้ (User-Centric) พัฒนาด้วย Kotlin สำหรับ Android โดยมีจุดเด่นอยู่ที่ความยืดหยุ่นในการรัน JavaScript และการจัดการหน้าจอที่อิสระ

## ✨ ฟีเจอร์หลัก (Key Features)

- **Draggable & Snappable FAB**: ปุ่มลอยอัจฉริยะที่สามารถลากไปวางจุดไหนของจอก็ได้ และจะ "Snap" ดูดติดขอบจออัตโนมัติเมื่อปล่อยมือ เพื่อไม่ให้บดบังเนื้อหาเว็บ
- **Floating Control Panel**: แผงควบคุมแบบลอยตัวที่รวมทุกอย่างไว้ในที่เดียว (Back, Home, Refresh, URL Bar) เปิด-ปิดได้ผ่านปุ่ม FAB
- **Dual-Pane Mode (Split Screen)**: รองรับการเปิดเว็บ 2 หน้าจอพร้อมกัน พร้อมตัวแบ่งหน้าจอ (Splitter) ที่ลากปรับขนาดได้ตามต้องการ
- **Custom JavaScript Shortcuts**: 
    - เพิ่มสคริปต์ JS ส่วนตัวได้ไม่จำกัด
    - **Individual Eye Toggles**: เลือกเปิด/ปิด "ดวงตา" เพื่อแสดงเฉพาะสคริปต์ที่ต้องการบนหน้าจอหลัก
    - **Edit & Delete**: จัดการสคริปต์ได้ง่ายๆ เพียงกดค้างที่ปุ่มสคริปต์
- **Auto-run (Background Script)**: ตั้งค่าให้สคริปต์รันอัตโนมัติทุกครั้งที่โหลดหน้าเว็บเสร็จ (Background persistence)
- **Customizable Home URL**: ผู้ใช้สามารถตั้งค่าหน้าแรก (Home) ของตัวเองได้ผ่านเมนูตั้งค่า
- **Persistence System**: บันทึกสคริปต์และหน้าแรกไว้ในเครื่องถาวรด้วย SharedPreferences ปิดแอปข้อมูลไม่หาย

## 📁 โครงสร้างไฟล์ (Project Structure)

- `app/src/main/java/com/example/spiritwebview/BrowserActivity.kt`: โค้ดหลักที่ควบคุม UI, WebView และระบบจัดการสคริปต์
- `app/src/main/java/com/example/spiritwebview/AndroidBridge.kt`: ตัวกลางเชื่อมต่อระหว่าง JavaScript (บนเว็บ) และ Native (Android)
- `app/src/main/res/layout/activity_browser.xml`: ไฟล์เลย์เอาต์ที่รวม WebView และแผงควบคุมแบบ CardView
- `app/src/main/res/values/strings.xml`: กำหนดชื่อแอปเป็น "Gooser"
- `app/src/main/AndroidManifest.xml`: ตั้งค่าสิทธิ์การเข้าถึงอินเทอร์เน็ตและกำหนด Activity หลัก

## 🚀 การติดตั้งและใช้งาน (Setup & Usage)

1. **สิทธิ์การใช้งาน**: ตรวจสอบว่าได้เพิ่มสิทธิ์อินเทอร์เน็ตใน Manifest แล้ว:
   ```xml
   <uses-permission android:name="android.permission.INTERNET" />
   ```
2. **ViewBinding**: เปิดใช้งานใน `build.gradle (Module:app)`:
   ```gradle
   buildFeatures {
       viewBinding true
   }
   ```
3. **การเพิ่มสคริปต์**:
   - กดปุ่ม FAB เพื่อเปิดแผงควบคุม
   - กดปุ่ม **"+ เพิ่มสคริปต์"**
   - กรอกชื่อและโค้ด JavaScript
   - หากต้องการให้รันทุกครั้งที่โหลดหน้าเว็บ ให้ติ๊กช่อง **"รันอัตโนมัติ"**
4. **การแสดงผลหน้าหลัก**:
   - ในรายการสคริปต์ ให้กดที่ **ไอคอนรูปตา** ของสคริปต์ที่ต้องการ เพื่อให้ปุ่มนั้นไปปรากฏที่แถบทางลัดด้านบนของหน้าจอหลัก

## 🛠️ สำหรับนักพัฒนา (For Developers)
โปรเจกต์นี้เป็น **Open Source** ที่เน้นให้นักพัฒนาสามารถนำไปต่อยอดและแบ่งปันสคริปต์ JavaScript เพื่อเพิ่มความสามารถให้เบราเซอร์ได้ตามความต้องการส่วนบุคคล

---
**Gooser Browser** - *Your Browser, Your Control.* 🦈🕶️
